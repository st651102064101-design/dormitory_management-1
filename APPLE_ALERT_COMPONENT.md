# 🍎 Apple Alert Component

คอมโพเนนต์ Alert แบบ Apple-style (iOS/macOS) สำหรับใช้งานทุกหน้าในระบบ

## 📦 ตำแหน่งไฟล์
```
includes/apple_alert.php
```

## 🚀 วิธีใช้งาน

### 1. Import Component
เพิ่มบรรทัดนี้ก่อน `</body>` tag ในหน้าที่ต้องการใช้:

```php
<?php include_once __DIR__ . '/../includes/apple_alert.php'; ?>
```

**ตัวอย่างตาม path:**
```php
// หน้าใน Public/
<?php include_once __DIR__ . '/../includes/apple_alert.php'; ?>

// หน้าใน Manage/
<?php include_once __DIR__ . '/../includes/apple_alert.php'; ?>

// หน้าใน root
<?php include_once __DIR__ . '/includes/apple_alert.php'; ?>
```

### 2. เรียกใช้ Alert

#### วิธีที่ 1: ใช้ alert() แบบปกติ (แนะนำ)
Component จะ override `alert()` ของ JavaScript ให้เป็นแบบ Apple อัตโนมัติ:

```javascript
alert('กรุณากรอกข้อมูลให้ครบถ้วน');
```

#### วิธีที่ 2: เรียก showAppleAlert() โดยตรง
```javascript
showAppleAlert('ข้อความ', 'หัวข้อ');

// ตัวอย่าง
showAppleAlert('บันทึกข้อมูลสำเร็จ', 'สำเร็จ');
showAppleAlert('กรุณากรอกเบอร์โทรศัพท์', 'แจ้งเตือน');
```

#### วิธีที่ 3: ปิด Alert ด้วย code
```javascript
closeAppleAlert();
```

## 🎨 คุณสมบัติ

### ✨ Design Features
- 🔲 แบบ Apple iOS/macOS 100%
- 💫 Backdrop blur 10px + Dialog blur 30px
- 📐 Border radius 14px
- 🎨 Translucent white/dark background
- 🌓 รองรับ Dark Mode อัตโนมัติ
- 📱 Responsive ทุกขนาดหน้าจอ

### ⚡ Animation
- Fade in: 0.2s
- Scale + Bounce: 0.3s (cubic-bezier)
- Active state: scale 0.98

### 🎯 UX Features
- ✅ คลิกพื้นหลังเพื่อปิด
- ⌨️ กด Escape เพื่อปิด
- 🚫 Block scroll เมื่อเปิด
- 🔁 Override alert() ให้เป็นแบบ Apple

### 🎨 Color Scheme
- Button: **#007AFF** (iOS Blue)
- Typography: San Francisco style
- Letter-spacing: -0.4px (Title), -0.08px (Message)

## 📝 ตัวอย่างการใช้งานในแต่ละสถานการณ์

### Form Validation
```javascript
if (!roomSelected) {
    alert('กรุณาเลือกห้องพัก');
    return;
}

if (!phone || phone.length !== 10) {
    alert('กรุณากรอกเบอร์โทรศัพท์ 10 หลัก');
    return;
}
```

### Success Message
```javascript
showAppleAlert('บันทึกข้อมูลเรียบร้อยแล้ว', 'สำเร็จ');
```

### Error Message
```javascript
showAppleAlert('เกิดข้อผิดพลาดในการบันทึกข้อมูล', 'ข้อผิดพลาด');
```

### Confirmation Message
```javascript
showAppleAlert('กรุณาตรวจสอบข้อมูลก่อนส่ง', 'ยืนยัน');
```

## 📋 หน้าที่ใช้งานแล้ว

- ✅ `Public/booking.php` - หน้าจองห้อง
- ✅ `Public/booking_status.php` - หน้าตรวจสอบสถานะการจอง
- ✅ `index.php` - หน้าแรก
- ✅ `Tenant/index.php` - หน้าผู้เช่า

## 🔧 Customization

### เปลี่ยนข้อความหัวข้อ Default
แก้ไขใน `includes/apple_alert.php`:
```javascript
function showAppleAlert(message, title = 'หัวข้อที่คุณต้องการ') {
    // ...
}
```

### เปลี่ยนสีปุ่ม
แก้ไขใน `<style>`:
```css
.apple-alert-btn {
    color: #007AFF; /* เปลี่ยนเป็นสีที่ต้องการ */
}
```

### เปลี่ยนขนาด Dialog
```css
.apple-alert-dialog {
    max-width: 280px; /* เปลี่ยนตามต้องการ */
}
```

## 🐛 Troubleshooting

### Alert ไม่แสดง
1. ตรวจสอบว่าได้ include component แล้ว
2. เช็ค console มี error หรือไม่
3. ตรวจสอบ z-index (component ใช้ 99999)

### Animation ไม่ smooth
- ตรวจสอบ browser รองรับ `backdrop-filter`
- Safari/Chrome: รองรับ
- Firefox: ต้องเปิด flag

### Dark Mode ไม่ทำงาน
- ใช้ `prefers-color-scheme: dark` media query
- ตรวจสอบ OS settings

## 💡 Tips

1. **ใช้ alert() ตามปกติ** - Component จะ override ให้เป็นแบบ Apple อัตโนมัติ
2. **ไม่ต้อง import ทุกหน้า** - เพิ่มเฉพาะหน้าที่ใช้งาน
3. **ข้อความสั้นกระชับ** - Alert เหมาะกับข้อความสั้นๆ
4. **ใช้ title ที่ชัดเจน** - ช่วยให้ user เข้าใจง่าย

## 📚 อ้างอิง

- [Apple Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/)
- iOS Alert Style
- macOS Dialog Style

---

**สร้างโดย:** GitHub Copilot  
**เวอร์ชัน:** 1.0.0  
**อัพเดทล่าสุด:** 7 มกราคม 2568
