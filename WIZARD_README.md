# คู่มือติดตั้งและใช้งานระบบ Wizard - จัดการผู้เช่า

## 📋 สารบัญ
- [ภาพรวมระบบ](#ภาพรวมระบบ)
- [การติดตั้ง](#การติดตั้ง)
- [โครงสร้างไฟล์](#โครงสร้างไฟล์)
- [คู่มือการใช้งาน](#คู่มือการใช้งาน)
- [FAQ](#faq)
- [Troubleshooting](#troubleshooting)

---

## 🎯 ภาพรวมระบบ

ระบบ Wizard นี้ช่วยให้ Admin จัดการผู้เช่าได้อย่างเป็นขั้นตอน ครอบคลุมตั้งแต่การจองห้องจนถึงการออกบิลรายเดือน

### ขั้นตอนทั้งหมด 5 ขั้นตอน:

```
1. ยืนยันจอง → 2. ยืนยันชำระเงินจอง → 3. สร้างสัญญา → 4. เช็คอิน → 5. เริ่มบิลรายเดือน
```

### คุณสมบัติหลัก:
- ✅ ติดตามสถานะผู้เช่าแบบ real-time
- ✅ ไม่พลาดขั้นตอนสำคัญ
- ✅ บันทึกประวัติการทำงานทุกขั้นตอน
- ✅ UI ที่ใช้งานง่าย มีสัญลักษณ์ชัดเจน
- ✅ อัตโนมัติเต็มรูปแบบ

---

## 🚀 การติดตั้ง

### ขั้นตอนที่ 1: เตรียม Backup (สำคัญ!)

```bash
# Backup database ก่อนทำการติดตั้ง
mysqldump -u root -p dormitory_management > backup_$(date +%Y%m%d_%H%M%S).sql
```

### ขั้นตอนที่ 2: รัน SQL Script

เลือกวิธีใดวิธีหนึ่ง:

**วิธีที่ 1: ผ่าน MySQL Command Line**
```bash
cd /Applications/XAMPP/xamppfiles/htdocs/dormitory_management
mysql -u root -p dormitory_management < database_wizard_updates.sql
```

**วิธีที่ 2: ผ่าน phpMyAdmin**
1. เปิด phpMyAdmin (http://localhost/phpmyadmin)
2. เลือก database `dormitory_management`
3. คลิก tab "SQL"
4. Copy เนื้อหาจาก `database_wizard_updates.sql` แล้ว Paste
5. กด "Go"

### ขั้นตอนที่ 3: ตรวจสอบการติดตั้ง

```sql
-- ตรวจสอบว่าตารางถูกสร้างสำเร็จ
SHOW TABLES LIKE '%booking_payment%';
SHOW TABLES LIKE '%checkin_record%';
SHOW TABLES LIKE '%tenant_workflow%';

-- ตรวจสอบ View
SHOW CREATE VIEW vw_tenant_wizard;
```

### ขั้นตอนที่ 4: สร้างโฟลเดอร์สำหรับอัปโหลดรูปภาพ

```bash
mkdir -p /Applications/XAMPP/xamppfiles/htdocs/dormitory_management/Public/Assets/Images/Checkin
chmod 755 /Applications/XAMPP/xamppfiles/htdocs/dormitory_management/Public/Assets/Images/Checkin
```

### ขั้นตอนที่ 5: ทดสอบระบบ

1. เข้าสู่ระบบในฐานะ Admin
2. คลิกเมนู **"จัดการผู้เช่า (Wizard)"** ใน Sidebar
3. หากไม่มี error แสดงว่าติดตั้งสำเร็จ!

---

## 📁 โครงสร้างไฟล์

### ไฟล์ที่สร้างใหม่:

```
dormitory_management/
├── database_wizard_updates.sql          # SQL script สำหรับสร้างตาราง
├── WIZARD_DESIGN.md                     # เอกสารออกแบบ
├── WIZARD_README.md                     # คู่มือนี้
├── includes/
│   └── wizard_helper.php                # Helper functions
├── Reports/
│   ├── tenant_wizard.php                # หน้าหลัก Wizard
│   ├── wizard_step1.php                 # Step 1: ยืนยันจอง
│   ├── wizard_step2.php                 # Step 2: ยืนยันชำระเงินจอง
│   ├── wizard_step3.php                 # Step 3: สร้างสัญญา
│   ├── wizard_step4.php                 # Step 4: เช็คอิน
│   └── wizard_step5.php                 # Step 5: เริ่มบิลรายเดือน
└── Manage/
    ├── process_wizard_step1.php         # ประมวลผล Step 1
    ├── process_wizard_step2.php         # ประมวลผล Step 2
    ├── process_wizard_step3.php         # ประมวลผล Step 3
    ├── process_wizard_step4.php         # ประมวลผล Step 4
    └── process_wizard_step5.php         # ประมวลผล Step 5
```

### ไฟล์ที่แก้ไข:

```
includes/sidebar.php                     # เพิ่มเมนู Wizard
```

---

## 📖 คู่มือการใช้งาน

### การเริ่มต้นใช้งาน

#### สถานการณ์: มีผู้เช่าใหม่ต้องการจองห้อง

**ขั้นตอนเริม:**
1. สร้างข้อมูลผู้เช่าใน "จัดการผู้เช่า"
2. สร้างการจองใน "การจองห้อง"
3. เมื่อจองสำเร็จ จะมีรายการปรากฏใน **"จัดการผู้เช่า (Wizard)"** อัตโนมัติ

---

### Step 1: ยืนยันจอง 🎫

**จุดประสงค์:**
- อนุมัติการจองของผู้เช่า
- ล็อกห้องไม่ให้คนอื่นจองซ้ำ

**ขั้นตอน:**
1. เปิดหน้า **"จัดการผู้เช่า (Wizard)"**
2. หารายการที่อยู่ในสถานะ Step 1
3. กดปุ่ม **"ยืนยันจอง"**
4. ตรวจสอบข้อมูล แล้วกด **"✓ ยืนยันการจอง"**

**ผลลัพธ์:**
- ✅ ห้องถูกล็อก (room_status = 2)
- ✅ สร้างยอดเงินจอง 2,000 บาท
- ✅ อัปเดตสถานะผู้เช่าเป็น "จองห้อง"
- ✅ สร้าง Workflow Record

---

### Step 2: ยืนยันชำระเงินจอง 💰

**จุดประสงค์:**
- ตรวจสอบหลักฐานการชำระเงินจอง
- ออกใบเสร็จ

**ขั้นตอน:**
1. รายการจะขึ้นสถานะ Step 2
2. กดปุ่ม **"ยืนยันชำระเงินจอง"**
3. ตรวจสอบหลักฐาน (ถ้ามี)
4. กด **"✓ ยืนยันการชำระเงิน"**

**ผลลัพธ์:**
- ✅ บันทึกวันที่ชำระเงิน
- ✅ สร้างเลขที่ใบเสร็จอัตโนมัติ (รูปแบบ: RC20260127XXXX)
- ✅ เปลี่ยนสถานะเป็น "ชำระแล้ว"

---

### Step 3: สร้างสัญญา 📄

**จุดประสงค์:**
- สร้างสัญญาเช่าอย่างเป็นทางการ

**ขั้นตอน:**
1. กดปุ่ม **"สร้างสัญญา"**
2. กรอกข้อมูล:
   - วันเริ่มสัญญา (default = วันนี้)
   - ระยะเวลา (3, 6 หรือ 12 เดือน)
   - เงินประกัน (default = 2,000 บาท)
3. กด **"✓ สร้างสัญญา"**

**ผลลัพธ์:**
- ✅ สร้างสัญญาในฐานข้อมูล
- ✅ คำนวณวันสิ้นสุดอัตโนมัติ
- ✅ อัปเดตสถานะห้องเป็น "ไม่ว่าง"
- ✅ อัปเดตสถานะผู้เช่าเป็น "รอเข้าพัก"

---

### Step 4: เช็คอิน 🔑

**จุดประสงค์:**
- บันทึกมิเตอร์น้ำ-ไฟเริ่มต้น
- ถ่ายรูปสภาพห้อง
- บันทึกการส่งมอบกุญแจ

**ขั้นตอน:**
1. กดปุ่ม **"เช็คอิน"**
2. กรอกข้อมูล:
   - วันที่เช็คอิน
   - **มิเตอร์น้ำเริ่มต้น** (สำคัญ!)
   - **มิเตอร์ไฟเริ่มต้น** (สำคัญ!)
   - เลขกุญแจ
   - รูปสภาพห้อง (optional, หลายรูป)
   - หมายเหตุ (optional)
3. กด **"✓ บันทึกเช็คอิน"**

**ผลลัพธ์:**
- ✅ บันทึกเลขมิเตอร์เริ่มต้น
- ✅ อัปโหลดรูปสภาพห้อง
- ✅ อัปเดตสถานะผู้เช่าเป็น **"พักอยู่"** 🎉

**⚠️ หมายเหตุ:** เลขมิเตอร์เริ่มต้นจะใช้สำหรับคำนวณค่าน้ำ-ไฟในเดือนถัดไป

---

### Step 5: เริ่มบิลรายเดือน 📊

**จุดประสงค์:**
- สร้างบิลรายเดือนแรก
- เปิดระบบเรียกเก็บเงินอัตโนมัติ

**ขั้นตอน:**
1. กดปุ่ม **"เริ่มบิลรายเดือน"**
2. ตรวจสอบข้อมูล:
   - รอบบิลแรก (เดือนถัดไป)
   - อัตราค่าน้ำ-ไฟ
3. กด **"🎉 เริ่มบิลรายเดือนและเสร็จสิ้น"**

**ผลลัพธ์:**
- ✅ สร้างบิลรายเดือนแรก (เดือนถัดไป)
- ✅ เปิดใช้งานระบบคำนวณอัตโนมัติ
- ✅ **เสร็จสิ้นกระบวนการ Wizard!** 🎊
- ✅ ผู้เช่าพร้อมเข้าพักและใช้งานระบบได้เต็มรูปแบบ

---

## ❓ FAQ (คำถามที่พบบ่อย)

### Q1: ถ้าทำ Step ผิดลำดับจะเป็นอย่างไร?
**A:** ระบบป้องกันไม่ให้ข้ามขั้นตอน ต้องทำตามลำดับ 1→2→3→4→5 เท่านั้น

### Q2: สามารถย้อนกลับแก้ไขขั้นตอนก่อนหน้าได้ไหม?
**A:** ขณะนี้ยังไม่รองรับการย้อนแก้ไข แต่สามารถแก้ไขข้อมูลได้จากหน้าจัดการตามปกติ

### Q3: ถ้าผู้เช่ายกเลิกการจองระหว่างทาง จะทำอย่างไร?
**A:** ลบการจองจากหน้า "การจองห้อง" ระบบจะอัปเดต Workflow อัตโนมัติ

### Q4: ทำไมต้องบันทึกเลขมิเตอร์เริ่มต้น?
**A:** เพื่อใช้คำนวณค่าน้ำ-ไฟในเดือนถัดไป โดยระบบจะหักค่าที่ใช้ไป = เลขปัจจุบัน - เลขเริ่มต้น

### Q5: สามารถข้าม Step 4 (เช็คอิน) ได้ไหม?
**A:** ไม่ได้ เพราะต้องมีเลขมิเตอร์เริ่มต้นสำหรับคำนวณค่าน้ำ-ไฟ

### Q6: บิลรายเดือนจะถูกสร้างอัตโนมัติหรือไม่?
**A:** ใช่! หลังจากทำ Step 5 เสร็จแล้ว ระบบ auto-generate expense จะสร้างบิลทุกต้นเดือนอัตโนมัติ

---

## 🔧 Troubleshooting

### ปัญหา: ไม่พบเมนู "จัดการผู้เช่า (Wizard)"

**วิธีแก้:**
```bash
# ตรวจสอบว่าไฟล์ sidebar.php มีการเพิ่มเมนูหรือยัง
grep -n "tenant_wizard" /Applications/XAMPP/xamppfiles/htdocs/dormitory_management/includes/sidebar.php
```

หากไม่พบ ให้เพิ่มโค้ดนี้ใน sidebar.php หลังเมนู "การจองห้อง":
```html
<a class="" href="tenant_wizard.php">
    <span class="app-nav-icon" aria-hidden="true">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="10"/>
            <path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/>
        </svg>
    </span>
    <span class="app-nav-label">จัดการผู้เช่า (Wizard)</span>
</a>
```

---

### ปัญหา: Error "Table 'booking_payment' doesn't exist"

**วิธีแก้:**
```sql
-- รัน SQL script อีกครั้ง
SOURCE /Applications/XAMPP/xamppfiles/htdocs/dormitory_management/database_wizard_updates.sql;

-- หรือ
mysql -u root -p dormitory_management < database_wizard_updates.sql
```

---

### ปัญหา: ไม่สามารถอัปโหลดรูปภาพใน Step 4 ได้

**วิธีแก้:**
```bash
# ตรวจสอบว่ามีโฟลเดอร์หรือไม่
ls -la /Applications/XAMPP/xamppfiles/htdocs/dormitory_management/Public/Assets/Images/

# สร้างโฟลเดอร์ใหม่
mkdir -p /Applications/XAMPP/xamppfiles/htdocs/dormitory_management/Public/Assets/Images/Checkin

# ตั้งค่า permission
chmod 755 /Applications/XAMPP/xamppfiles/htdocs/dormitory_management/Public/Assets/Images/Checkin
```

---

### ปัญหา: Workflow ไม่อัปเดต

**วิธีแก้:**
```sql
-- ตรวจสอบ Workflow Record
SELECT * FROM tenant_workflow WHERE tnt_id = 'YOUR_TNT_ID';

-- ลบและสร้างใหม่ (ถ้าจำเป็น)
DELETE FROM tenant_workflow WHERE tnt_id = 'YOUR_TNT_ID';
```

---

### ปัญหา: หน้า Wizard แสดงข้อมูลไม่ครบ

**วิธีแก้:**
```sql
-- ตรวจสอบ View
SELECT * FROM vw_tenant_wizard LIMIT 10;

-- สร้าง View ใหม่ (ถ้า error)
DROP VIEW IF EXISTS vw_tenant_wizard;
-- แล้วรัน SQL จาก database_wizard_updates.sql อีกครั้ง
```

---

## 📞 ติดต่อและสนับสนุน

หากพบปัญหาหรือต้องการความช่วยเหลือ:

1. ตรวจสอบ error log:
   ```bash
   tail -f /Applications/XAMPP/xamppfiles/logs/php_error_log
   ```

2. เปิด Browser Console (F12) เพื่อดู JavaScript error

3. ตรวจสอบ database log:
   ```sql
   SHOW ENGINE INNODB STATUS;
   ```

---

## 🎉 ขอบคุณที่ใช้งาน!

ระบบ Wizard นี้ออกแบบมาเพื่อทำให้การจัดการผู้เช่าง่ายและมีประสิทธิภาพที่สุด

**Happy Coding! 🚀**

---

*เอกสารนี้อัปเดตล่าสุด: 27 มกราคม 2026*
